import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    include: ['test/**/*.test.js'],
    environment: 'node',
    // Отключаем keep-alive пул undici (см. комментарий в setup.js) — снижает флейки HTTP-тестов.
    setupFiles: ['./test/helpers/setup.js'],
    // Редкий (~1/18) СРЕДОВОЙ флейк: переиспользование эфемерного порта + пул соединений под
    // параллельной нагрузкой изредка доставляет запрос «не на тот» сервер. Это самокорректируемая
    // гонка окружения (на свежем соединении проходит), поэтому 2 ретрая её гасят. Реальный баг в
    // обработчике детерминирован и провалит все попытки — маскировки логики тут нет.
    retry: 2,
    // Интеграционные тесты поднимают реальные сокеты/таймеры — даём запас по времени.
    testTimeout: 10000,
    hookTimeout: 10000,
  },
});
